<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= lang === 'zh' ? '修改密码' : 'Change Password' %></title>
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    body {
      background: #f0f0f0;
      font-family: Arial, sans-serif;
    }
    .change-box {
      background: #fff;
      width: 100%;
      max-width: 500px;
      margin: 80px auto;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }
    .change-box h2 {
      text-align: center;
      margin-bottom: 25px;
      font-weight: bold;
    }
    .form-group {
      margin-bottom: 15px;
    }
    .btn-primary {
      background-color: #007bff;
      border: none;
      padding: 10px;
      width: 100%;
      color: #fff;
      font-weight: bold;
      border-radius: 5px;
      cursor: pointer;
    }
    .btn-primary:hover {
      background-color: #0056b3;
    }
    .text-center {
      text-align: center;
    }
    .lang-switch {
      text-align: center;
      margin-top: 15px;
    }
  </style>
</head>
<body>
  <div class="change-box">
    <h2><%= lang === 'zh' ? '修改密码' : 'Change Password' %></h2>

    <% if (message) { %>
      <p style="color:green;text-align:center;"><%= message %></p>
    <% } %>
    <% if (error) { %>
      <p style="color:red;text-align:center;"><%= error %></p>
    <% } %>

    <form action="/change-password" method="POST">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <div class="form-group">
        <label><%= lang === 'zh' ? '当前密码' : 'Current Password' %></label>
        <input type="password" name="currentPassword" class="form-control" required/>
      </div>
      <div class="form-group">
        <label><%= lang === 'zh' ? '新密码' : 'New Password' %></label>
        <input type="password" name="newPassword" class="form-control" required/>
      </div>
      <div class="form-group">
        <label><%= lang === 'zh' ? '确认新密码' : 'Confirm New Password' %></label>
        <input type="password" name="confirmPassword" class="form-control" required/>
      </div>
      <button type="submit" class="btn-primary"><%= lang === 'zh' ? '提交修改' : 'Change Password' %></button>
    </form>

    <div class="text-center mt-3">
      <a href="/installment"><%= lang === 'zh' ? '返回首页' : 'Back to Home' %></a>
    </div>

    <div class="lang-switch">
      <form action="/change-language" method="POST">
        <select name="lang" onchange="this.form.submit()">
          <option value="en" <%= lang === 'en' ? 'selected' : '' %>>English</option>
          <option value="zh" <%= lang === 'zh' ? 'selected' : '' %>>中文</option>
        </select>
      </form>
    </div>
  </div>
</body>
</html>
