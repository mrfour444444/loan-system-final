<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= lang === 'zh' ? '找回密码' : 'Forgot Password' %></title>
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    body {
      background: #f7f7f7;
      font-family: Arial, sans-serif;
    }
    .forgot-box {
      background: #fff;
      width: 100%;
      max-width: 400px;
      margin: 80px auto;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }
    .forgot-box h2 {
      text-align: center;
      margin-bottom: 25px;
      font-weight: bold;
    }
    .form-group {
      margin-bottom: 15px;
    }
    .btn-primary {
      background-color: #007bff;
      border: none;
      padding: 10px;
      width: 100%;
      color: #fff;
      font-weight: bold;
      border-radius: 5px;
      cursor: pointer;
    }
    .btn-primary:hover {
      background-color: #0056b3;
    }
    .text-center {
      text-align: center;
    }
    .lang-switch {
      text-align: center;
      margin-top: 15px;
    }
  </style>
</head>
<body>
  <div class="forgot-box">
    <h2><%= lang === 'zh' ? '找回密码' : 'Forgot Password' %></h2>

    <% if (message) { %>
      <p style="color:green;text-align:center;"><%= message %></p>
    <% } %>
    <% if (error) { %>
      <p style="color:red;text-align:center;"><%= error %></p>
    <% } %>

    <form action="/forgot-password" method="POST">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <div class="form-group">
        <label><%= lang === 'zh' ? '输入你的注册邮箱' : 'Enter your registered email' %></label>
        <input type="email" name="email" class="form-control" required/>
      </div>
      <button type="submit" class="btn-primary"><%= lang === 'zh' ? '发送新密码' : 'Send New Password' %></button>
    </form>

    <div class="text-center mt-3">
      <a href="/admin-login"><%= lang === 'zh' ? '返回登录' : 'Back to Login' %></a>
    </div>

    <div class="lang-switch">
      <form action="/change-language" method="POST">
        <select name="lang" onchange="this.form.submit()">
          <option value="en" <%= lang === 'en' ? 'selected' : '' %>>English</option>
          <option value="zh" <%= lang === 'zh' ? 'selected' : '' %>>中文</option>
        </select>
      </form>
    </div>
  </div>
</body>
</html>
